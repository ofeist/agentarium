apiVersion: agentarium.dev/v0
kind: Agent

metadata:
  name: math-agent
  version: 0.1.0

spec:
  purpose: Solve simple math tasks from structured input
  inputs:
    - expression
  outputs:
    - result
  prompt_ref: prompts/math.md
  mcp_dependencies: []
  a2a:
    role: math
    exposed: true
  runtime_type: packaged-agent
